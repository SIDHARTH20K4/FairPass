# MongoDB Connection
MONGODB_URI=

# Server Configuration
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# CORS Configuration
# For development
CORS_ORIGIN=http://localhost:3000
# For production (comma-separated if multiple)
# CORS_ORIGIN=https://fairpass.vercel.app,https://www.fairpass.vercel.app,
https://fairpass.onrender.com

# Optional: JWT Secret (for future auth)
JWT_SECRET=
